apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: server-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/server-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/server-default: "true"
